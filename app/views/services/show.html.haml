%h2 #{@service.name}

%ul#info
  %li
    %b Service:
    = @service.name
  %li
    %b Category:
    = @service.category
  %li
    %b Price:
    = @service.price
    
%h4 Details:

%p
  %b Location: 
  =@service.location
%p
  %b Rating:
  =@service.rating

%p 
= link_to 'Edit', edit_service_path(@service), :class => 'btn btn-primary'

= link_to 'Delete', service_path(@service), 'data-method' => 'delete', 'data-confirm' => 'Delete service?', :class => 'btn btn-secondary'

%p
= link_to 'Back', services_path